pyrealsense2
pymodbus==2.5.3
ctranslate2==4.4.0

# YOLOE-26 open-vocabulary perception runtime. YOLOE-26 requires >=8.4.0.
ultralytics==8.4.142
# Keep Torch and TorchVision matched to the installed CUDA runtime.
# Install the pair separately; do not pin torchvision here.
ultralytics-thop==2.1.6
git+https://github.com/ultralytics/CLIP.git
ftfy==6.3.1
